# anki-auto-bold-target
Automatically bold the target word in the sentence at review time without touching your templates.   Works with JPMN and previously converted cards. Handles kanji + kana conjugations, kana-only words, and even cards that already wrap targets with `<t>…</t>`.
